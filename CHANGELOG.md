# 更新日志

## 1.4.1

- 修复切换到蒙古语、日语、阿拉伯语等非中文目标语言后仍显示中文释义的问题。
- 新增分层目标语翻译路径，按批次请求所选语言并在主服务限流时自动切换备用服务。
- 非中文目标语请求失败时不再注入或缓存中文释义；会保留已有目标语结果并自动重试。
- 整理仓库资源和忽略规则，移除构建缓存、旧发布包、语言包中间文件与冗余 Logo，并补充第三方词典许可声明。

## 1.4.0

- 软件界面语言扩展到与目标翻译相同的近 100 种全球语言，并补充爪哇语、普什图语、库尔德语、奥罗莫语、海地克里奥尔语等高影响语种。
- 98 种软件界面语言全部改为静态内置语言包，可离线即时切换，不再首用联网生成或启动后台翻译进程。
- 优化软件语言和目标翻译语言切换路径，隐藏必要的后台进程并加入草稿级目标译文缓存。
- 修复部分 WebView 中首次切换语言后下拉框保持禁用、必须重启才能继续切换的问题。
- 修复公共翻译服务返回 429 时泄露 PowerShell 错误堆栈的问题；限流或断网时会立即使用本地 ECDICT 中文释义与 IPA，并进入 5 分钟冷却，避免重复请求。
- 继续采用内置自动多语翻译，不提供用户 API 或网络词库配置。

## 1.3.0

- 新增软件界面语言选项，支持简体中文与 English，并会记住用户选择。
- 内置自动多语翻译，用户无需配置接口地址、模型或密钥。
- 简体中文释义与注音支持离线生成；其他目标语言首次生成时需要联网，生成结果会缓存到当前草稿。
- 修复切换目标语言或注音方式后内容不显示的问题，美式 IPA、英式近似 IPA、通用 IPA、易读拉丁转写及音节重音现均可正常显示。
- 删除用户 API 配置和网络词库接入入口，简化翻译与注音操作流程。

## 1.2.0

- 软件正式更名为“语界精读”，加入全新书页、对话气泡与地球组合 Logo，并嵌入 Windows EXE 图标资源。
- 新增 70 余种目标翻译语言，覆盖主要亚洲、欧洲、西亚与非洲语种。
- 新增美式/英式/通用 IPA、易读拉丁转写、音节重音和关闭注音。
- 语言与注音选择会随草稿保存，并同步影响预览、自动多语标注、DOCX 与 PDF。
- 非中文目标语不会混入离线中文释义；自动生成的同名词条会替换旧译文。

## v2026.7.27（应用版本 1.1.0）

### 桌面体验

- 默认在 Windows 软件窗口内显示界面，不再自动打开浏览器。
- 适配不同屏幕分辨率、Windows 显示缩放比例和软件窗口尺寸。
- 左、中、右三栏支持拖拽调宽并自动记忆，窄窗口自动恢复自适应布局。
- 英文文章输入框可纵向拖动调整高度。

### 标注与预览

- 标注预览增加缩小、放大、100%、适合宽度和放大查看模式。
- 英文文章中的高亮会按具体单词位置即时同步到标注预览。
- 普通单词和带音标单词均支持预览高亮。
- 支持多色高亮、词汇标注、句子解析和段落批注。

### 编辑功能

- 增加撤销、重做、查找替换、格式清理和英文规范。
- 增加实时字数、句数、段落数和预计阅读时间统计。
- 增加草稿自动保存、最近 20 个历史版本和恢复功能。
- 增加词汇批量管理、重点词、忽略词和本地选词词典。

### 输出与修复

- DOCX、PDF 与实时预览保持相同的页面尺寸和排版设置。
- 修复预览区普通单词无法同步高亮的问题。
- 优化低分辨率、小窗口和高 DPI 环境下的布局稳定性。

Windows 用户可下载 Release 中的便携版 ZIP，解压后直接运行 `语界精读.exe`。
