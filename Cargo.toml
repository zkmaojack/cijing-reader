[package]
name = "yujie-reader"
version = "1.4.1"
edition = "2024"

[dependencies]

[target.'cfg(windows)'.dependencies]
wry = { version = "0.55.1", default-features = false, features = ["os-webview"] }
