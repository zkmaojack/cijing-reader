[package]
name = "rust-cijing-reader"
version = "1.1.0"
edition = "2024"

[dependencies]

[target.'cfg(windows)'.dependencies]
wry = { version = "0.55.1", default-features = false, features = ["os-webview"] }
